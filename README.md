# Figma ComponentCraft 插件 v1.2.1

这是一个Figma插件，允许用户上传PNG图片，填写组件名称和描述，然后自动将图片等比裁剪为300x300px并创建为组件。

## 功能特点

- 上传PNG图片
- 填写组件名称和描述
- 自动等比裁剪图片至300x300px
- 创建命名组件并添加描述
- 实时预览上传的图片
- 支持为组件名称添加统一前缀（可开启/关闭）
- 自动记忆上一次使用的前缀字段
- 新增悬浮上传按钮，常驻于面板右下方，方便快速上传图片
- 自动提取上传的图片文件名并填充到组件名称中

## 安装方法

1. 在Figma中打开插件管理界面
2. 点击"开发"选项卡
3. 点击"+"按钮，选择"导入插件"
4. 选择本插件的`manifest.json`文件

## 使用方法

1. 在Figma中打开或创建一个文件
2. 运行"ComponentCraft"插件
3. 点击"选择图片"按钮，上传PNG格式的图片
4. 填写组件名称和描述
5. 点击"创建组件"按钮
6. 插件将自动创建300x300px的组件，并将图片等比缩放居中显示

## 技术实现

- 使用Figma插件API创建组件和图片节点
- 实现等比缩放算法，确保图片保持原始比例
- 提供用户友好的界面，支持实时预览
- 包含完整的错误处理和用户反馈机制

## 文件结构

```
figma-image-component-plugin/
├── manifest.json     # 插件配置文件
├── code.js          # 插件主代码
├── ui.html          # 插件界面
└── README.md        # 说明文档

## 版本历史

### v1.2.1 (2026-03-18)
- 上传图片后自动提取文件名并填充到组件名称
- 修复删除全部图片后仍显示禁用态“创建组件”按钮的问题

### v1.2 (2026-03-17)
- 新增悬浮上传按钮，常驻于面板右下方
- 悬浮按钮支持快速上传图片，与主上传按钮功能一致
- 悬浮按钮在达到最大图片数量（5张）时自动变为灰色禁用状态
- 优化按钮状态管理，确保禁用状态下无法触发上传
- 修复创建组件后按钮文案显示错误的问题

### v1.1 (2026-03-16)
- 新增组件名称前缀功能（可开启/关闭）
- 新增前缀记忆功能，自动记录上一次使用的前缀
- 优化UI交互逻辑
- 修复存储兼容性问题

### v1.0 (2026-03-16)
- 初始版本发布
- 支持上传PNG图片创建组件
- 支持批量创建组件（最多5个）
- 实现图片等比缩放和居中显示
- 提供组件名称和描述功能